name: Build Tweak
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y git curl make unzip wget

      - name: Setup Theos
        run: |
          export THEOS=/home/runner/theos
          curl -fsSL https://theos.dev/install.sh | sh
          echo "THEOS=/home/runner/theos" >> $GITHUB_ENV

      - name: Download iPhoneOS15.2.sdk
        run: |
          mkdir -p /home/runner/theos/sdks
          cd /home/runner/theos/sdks
          wget -O sdk.zip https://github.com/xybp888/iOS-SDKs/raw/master/iPhoneOS15.2.sdk.zip
          unzip -q sdk.zip
          rm -f sdk.zip

      - name: Compile tweak
        run: |
          export THEOS=/home/runner/theos
          ls -la $THEOS/makefiles/
          make package

      - name: Export .deb
        uses: actions/upload-artifact@v4
        with:
          name: built-deb
          path: packages/*.deb